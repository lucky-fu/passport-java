<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.example.demo.mapper.User">
    <insert id="insert" parameterType="com.example.demo.model.User">
        INSERT INTO USER(id,name, avatar, introduce, status, extension, create_time, modify_time) VALUES(#{id}, #{name}, #{avatar}, #{introduce}, #{status}, #{extension}, #{createTime}, #{modifyTime})
    </insert>
    <update id="update" parameterType="com.example.demo.model.User">
        UPDATE USER
        SET name=#{name}, avatar=#{avatar}, introduce=#{introduce}, status={#status}, extension={#extension}, create_time={#createTime},modify_time={#modfiyTime}
        WHERE id=#{id}
    </update>
    <delete id="delete" parameterType="com.example.demo.model.User">
        DELETE USER
            WHERE id=#{id}
    </delete>
    <select id="select" parameterType="com.example.demo.model.User">
        SELECT * FROM USER
            WHERE id=#{id}
    </select>
</mapper>